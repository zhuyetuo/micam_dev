# Copy this file to .env and adjust if needed:
#   cp .env.example .env
#
# Camera credentials are NOT stored here - go2rtc's built-in Xiaomi source
# handles login/token refresh itself and saves them into go2rtc/go2rtc.yaml
# after you add the camera from the WebUI (http://<host>:1984). See README.md.

TZ=Asia/Shanghai
